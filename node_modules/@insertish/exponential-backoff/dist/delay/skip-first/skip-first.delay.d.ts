import { Delay } from "../delay.base";
export declare class SkipFirstDelay extends Delay {
    apply(): Promise<unknown>;
    private get isFirstAttempt();
    protected get numOfDelayedAttempts(): number;
}
